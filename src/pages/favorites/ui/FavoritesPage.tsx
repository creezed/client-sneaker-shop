import { Heading } from '@chakra-ui/react';

const FavoritesPage = () => {
  return (
    <Heading mt={10} fontSize="36px" color="text.heading" fontWeight={300}>
      Favorites
    </Heading>
  );
};

// eslint-disable-next-line import/no-default-export
export default FavoritesPage;
